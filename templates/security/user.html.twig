<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Users</title>
    </head>
    <body>
        <table>
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Role</th>
            </tr>
            {% for user in listUser %}
            <tr>
                <td>{{user.id}}</td>
                <td>{{user.email}}</td>
                <td>
                    {% for rol in user.rol %}
                        {{ rol }}
                    {% endfor %}
                </td>
            </tr>
            {% endfor %}
        </table>
    </body>
</html>